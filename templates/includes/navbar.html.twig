<nav class="fixed-top navbar navbar-expand-lg navbar-light bg-light p-1">
  <a class="navbar-brand" href="{{ path('sorties_list') }}">Sortir.com</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" aria-current="page" href="{{ path('sorties_list') }}">Accueil</a>
      </li>
        {% if app.user %}
            {% if app.user.administrateur %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('nouveau_profil') }}">Créer utilisateur</a>
                </li>
            {% endif %}
                <li class="nav-item">
                    <a class="nav-link d-none d-sm-block" aria-current="page" href="{{ path('sorties_creerSortie') }}">Créer sortie</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('profil_afficher',{'id':app.user.id})}}" style="margin-right: 10px"> Mon profil </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('app_logout') }}" style="margin-right: 10px"> Se déconnecter({{ app.user.pseudonyme }}) </a>
                </li>
            {% else %}
            {# Si l'utilisateur n'est pas connecté #}
                <li class="nav-item">
                    <a class="nav-item nav-link" aria-current="page" href="{{ path('app_login') }}">Se connecter</a>
                </li>
        {% endif %}
    </ul>
  </div>
</nav>